#ifndef STATEEVENTLISTENER_H
#define STATEEVENTLISTENER_H

namespace chimera {
    namespace simulation {
        /*
        struct StateProviderArgs {
            size_t time_type;
            size_t state_type;
        };

        template<typename time_type, typename state_type>
        class StateEventListener:
            public IEventListener
        {
            public:
                StateEventListener() {}
                virtual ~StateEventListener() {}
                virtual void notify(const time_type& time, const state_type& state) = 0;
            protected:
            private:
        };
        */
    }
}

#endif // STATEEVENTLISTENER_H
